import { defineConfig } from "windicss/helpers";

export default defineConfig({
  theme: {},
  shortcuts: {
    "btn-style-common":
      "transform hover:(cursor-pointer scale-103 opacity-80 shadow-xm) active:(scale-97 opacity-100)",
  },
});
